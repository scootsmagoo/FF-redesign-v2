---
/** The side-by-side comparison now lives on the product page itself, as on the legacy site. */
const slug = Astro.params.slug ?? '';
return Astro.redirect(slug ? `/p/${encodeURIComponent(slug)}` : '/', 301);
---
